//import { IsOptional, IsUUID, Matches } from "class-validator"

export class Articulo {
    articuloId: string
    name: string
    descripcion: string
    price: number
    priceDesc: number
    stock: number
    usuarioId:string
   
}